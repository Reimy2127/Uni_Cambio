<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_change"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_change_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.UniChangeApp"
        tools:targetApi="31">
        <activity
            android:name=".Crear_anuncio"
            android:exported="false" />
        <activity
            android:name=".Registro_email"
            android:exported="false" /> <!-- Ahora opcioneslogin es la actividad principal -->
        <activity
            android:name=".opcioneslogin"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".login.LoginEmail"
            android:exported="true" />
        <activity
            android:name=".MainActivity"
            android:exported="true" />
    </application>

</manifest>